SECRET_KEY = '~.\xb83\x9a\xb0F\xe7\xdd(\xfb\xcc\xef)\xa8\x00\xd4X\xdel\xe4\xe8%\xb0'
SQLALCHEMY_DATABASE_URI = 'postgresql://test:test@postgres/roomanize'
SQLALCHEMY_TRACK_MODIFICATIONS = False
SQLALCHEMY_POOL_RECYCLE = True
APP_ENV = 'develop'

# for dev, we use sendmail so use config like
MAIL_MAILER = '/usr/sbin/ssmtp'
MAIL_MAILER_FLAGS = '-t'

# for prod we use smtp:
MAIL_SERVER = 'localhost'
MAIL_PORT = 25
MAIL_USE_TLS = True
MAIL_USE_SSL = False
MAIL_USERNAME = 'username'
MAIL_PASSWORD = 'password'
MAIL_MAX_EMAILS = None
MAIL_ASCII_ATTACHMENTS = False

# this is needed for both configs
MAIL_DEFAULT_SENDER = 'bot@roomanize.com'

# requires a caching config
# config options @https://flask-caching.readthedocs.io/en/latest/
CACHE_CONFIG = {
    'CACHE_TYPE': 'simple',
    'CACHE_KEY_PREFIX': 'local_dev',
}

TIME_ZONE = 'America/New_York'
# the following entry is used to test external urls for dev environments.
# this is used to test receipts upload functionality via smart phone
EXTERNAL_DEV_URL = '10.0.0.23'
